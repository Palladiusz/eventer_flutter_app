class EventModel {
  final int id;
  final String title;
  final String desc;
  final DateTime date;
  final bool checkedOut;

  EventModel({this.id, this.title, this.desc, this.date, this.checkedOut});
}
